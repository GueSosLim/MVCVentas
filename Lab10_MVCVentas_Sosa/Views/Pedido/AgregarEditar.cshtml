@model Modelo.PEDIDO
@{
    ViewBag.Title = (Model.IDPEDIDO > 0 ? Model.ESTADO : "Nuevo Registro");
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h2 class="page-header">Agregar/Editar Pedido</h2>

<ol class="breadcrumb">
    <li>
        <a href="~/Pedido/">Pedidos</a>
    </li>
    <li class="active">
        @(Model.IDPEDIDO > 0 ? Model.ESTADO : "Nuevo Registro")
    </li>
</ol>

@using (Html.BeginForm("Guardar", "Pedido", FormMethod.Post, new { id = "frm-categoria" }))
{
    @Html.HiddenFor(x => x.IDPEDIDO)

    <div class="panel panel-default">
        <div class="panel-heading">
            Información de la Categoría
        </div>
        <div class="panel-body">
            <div class="form-group">
                @Html.LabelFor(x => x.IDPEDIDO)
                @Html.TextBoxFor(x => x.IDPEDIDO, new { @class = "form-control", disabled = true })
                @Html.ValidationMessageFor(x => x.IDPEDIDO, null, new { @class = "label label-danger" })
            </div>
            <div>
                @Html.LabelFor(x => x.FECHA)
                @Html.TextBoxFor(x => x.FECHA, new { @class = "form-control" })
                @Html.ValidationMessageFor(x => x.FECHA, null, new { @class = "label label-danger" })
            </div>
            <div>
                @Html.LabelFor(x => x.ESTADO)
                <select name="estado" class="form-control">
                    <option @(Model.ESTADO == "A" ? "selected" : "") value="A">Activo</option>
                    <option @(Model.ESTADO == "I" ? "selected" : "") value="I">Inactivo</option>
                </select>
                @Html.ValidationMessageFor(x => x.ESTADO, null, new { @class = "label label-danger" })
            </div>
            <div>
                @Html.LabelFor(x => x.IDUSUARIO)
                @Html.TextAreaFor(x => x.IDUSUARIO, new { @class = "form-control" })
                @Html.ValidationMessageFor(x => x.IDUSUARIO, null, new { @class = "label label-danger" })
            </div>
        </div>
    </div>

    <div class="text-right">
        <button type="submit" class="btn btn-lg btn-primary"><span class="glyphicon glyphicon-floppy-save" aria-hidden="true"></span></button>
        <a href="~/Pedido/Index" class="btn btn-lg btn-danger"><span class="glyphicon glyphicon-floppy-remove" aria-hidden="true"></span></a>
    </div>
}